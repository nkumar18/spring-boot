package in.nareshit.raghu.Runner;

import java.util.Arrays;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

import in.nareshit.raghu.model.Book;
import in.nareshit.raghu.repository.BookRepository;

@Component
public class BookRepoRunner  implements CommandLineRunner {
	
	@Autowired
	private BookRepository repo;

	@Override
	public void run(String... args) throws Exception {
		// TODO Auto-generated method stub
		
		/*
		 * repo.saveAll( Arrays.asList( new Book(101, "Core Java", "SAM", 300.0,
		 * "BackEnd"), new Book(102, "Adv Java", "SAM", 400.0, "BackEnd"), new Book(103,
		 * "Spring Boot", "RAM", 500.0, "BackEnd"), new Book(104, "Angular", "RAM",
		 * 300.0, "FrontEnd"), new Book(105, "HTML", "SAM", 400.0, "FrontEnd"), new
		 * Book(106, "SQL/PLSQL", "RAM", 300.0, "Database") ) );
		 */
			


		
	}

}
